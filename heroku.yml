setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    api: microservice-project/api/DockerFile
    ui: microservice-project/ui/DockerFile

run:
  web: docker-compose up